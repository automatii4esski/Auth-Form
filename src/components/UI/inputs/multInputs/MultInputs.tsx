import { FC } from 'react';
import styles from './multInputs.module.scss';
import { MultInputProps } from '../../../../types/UI';

const MultInputs: FC<MultInputProps> = ({ className, value, ...props }) => {
  return <div>MultInputs</div>;
};

export default MultInputs;
